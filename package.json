{
  "name": "local-first-ai-rpg-runtime",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@9.15.9",
  "type": "module",
  "engines": {
    "node": ">=22 <25",
    "pnpm": ">=9 <12"
  },
  "scripts": {
    "dev": "vite",
    "typecheck": "tsc --noEmit",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage --no-file-parallelism --maxWorkers=1",
    "lint": "eslint .",
    "audit:prod": "pnpm audit --prod",
    "clean": "node scripts/clean.mjs",
    "rust:test": "cargo test --manifest-path src-tauri/Cargo.toml",
    "rust:clippy": "cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings",
    "rust:audit": "cd src-tauri && cargo audit",
    "verify": "pnpm typecheck && pnpm lint && pnpm test && pnpm build && pnpm audit:prod && pnpm rust:test && pnpm rust:clippy",
    "verify:release": "pnpm typecheck && pnpm lint && pnpm test:coverage && pnpm build && pnpm audit:prod && pnpm rust:audit && pnpm rust:test && pnpm rust:clippy && pnpm desktop:build",
    "verify:desktop": "pnpm verify && pnpm desktop:build",
    "tauri": "tauri",
    "desktop:open": "tauri dev",
    "desktop:dev": "tauri dev",
    "desktop:build": "tauri build"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0",
    "lucide-react": "^0.468.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@tauri-apps/cli": "^2.0.0",
    "@testing-library/jest-dom": "^6.6.3",
    "@testing-library/react": "^16.1.0",
    "@types/node": "^26.0.1",
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "@vitest/coverage-v8": "3.2.6",
    "eslint": "^10.6.0",
    "eslint-plugin-react-hooks": "^7.1.1",
    "globals": "^17.7.0",
    "jsdom": "^25.0.1",
    "typescript": "^5.6.3",
    "typescript-eslint": "^8.62.0",
    "vite": "^6.4.3",
    "vitest": "^3.2.6"
  }
}
