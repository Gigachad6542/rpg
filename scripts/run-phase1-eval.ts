// Stable TypeScript entrypoint retained for tooling and documentation. The
// implementation uses Vite's SSR loader so production modules can keep their
// extensionless TypeScript imports under Node.
import "./run-phase1-eval.mjs";
