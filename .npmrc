# Fail fast when the active Node or pnpm falls outside the `engines` ranges in
# package.json instead of only warning. Combined with Corepack and the pinned
# `packageManager` field, this keeps every environment on the exact supported
# toolchain (Node 22, pnpm 11.7.0).
engine-strict=true
